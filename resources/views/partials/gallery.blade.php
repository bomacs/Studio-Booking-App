{{-- <div class="container grid grid-cols-2 gap-2 mx-auto mt-20 px-2 md:grid-cols-3">
    @foreach ($galleryImages as $image)
    <div class="max-w-md rounded-sm shadow-md shadow-brightRedLight hover:scale-150 hover:origin-top transition duration-300 ease-in-out">
        <img src="{{ asset('imgs/gallery/' . $image->image_path)}}"
            alt="image">
    </div>
    @endforeach
</div>
<div class="font-semibold text-indigo-400 text-xs text-end mt-2 px-2.5 hover:text-indigo-900 md:text-sm md:mr-20">
    <a href="{{ route('gallery.index')}}">View More..</a>
</div>
 --}}
